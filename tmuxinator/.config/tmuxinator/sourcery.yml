# ~/.tmuxinator/sourcery.yml

name: sourcery
root: ~/sourcery-ai/sourcery

startup_window: zsh

windows:
  - editor: vim
  - shell:
      layout: even-horizontal
      panes:
        - # shell
        - status:
          - git fetch
          - git status
          - gh pr status
