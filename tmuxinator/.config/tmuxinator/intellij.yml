# ~/.tmuxinator/intellij.yml

name: intellij
root: ~/sourcery-ai/intellij-plugin/

startup_window: zsh

windows:
  - editor: vim
  - shell:
      layout: even-horizontal
      panes:
        - # shell
        - status:
          - git fetch
          - git status
          - gh pr status
