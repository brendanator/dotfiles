# ~/.tmuxinator/vscode.yml

name: vscode
root: ~/sourcery-ai/sourcery-vscode

startup_window: zsh

windows:
  - editor: vim
  - shell:
      layout: even-horizontal
      panes:
        - # shell
        - status:
          - git fetch
          - git status
          - gh pr status
